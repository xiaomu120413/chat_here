{
  "name": "chat_here",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/clear-dev-port.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "vite",
    "frontend:build": "vite build",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev --host",
    "ios:open": "tauri ios dev --open --host",
    "ios:build": "tauri ios build",
    "smoke:cli": "node scripts/smoke-cli.mjs",
    "smoke:mobile": "node scripts/smoke-mobile-gateway.mjs",
    "test": "node --test src/gateway/tests/*.test.js"
  },
  "dependencies": {
    "@tauri-apps/api": "2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.0.0",
    "vite": "^5.4.0"
  }
}
